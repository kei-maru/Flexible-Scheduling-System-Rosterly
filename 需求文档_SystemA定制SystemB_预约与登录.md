# 需求文档：Rosterly（System B 主体）与 System A 定制版

## 1. 产品定位

- 产品主体为 `Rosterly`（System B）。
- `System A` 不是平级系统，而是基于 `Rosterly` 核心能力做出的品牌化定制版前台。
- 业务规则、租户模型、排班/预约/订单/通知能力，原则上以 `Rosterly` 为核心沉淀。
- `System A` 负责品牌前台体验、特定业务编排与定制交互，不反向主导 `Rosterly` 的通用产品设计。

## 2. 产品目标

- 以 `Rosterly` 为主体，设计可复用的多租户排班与预约 SaaS。
- 支持店铺级开通、员工邀请注册、排班管理、订单管理、邮件通知、服务预设与店铺运营。
- 保持与 System A 当前核心业务能力大体一致，但将登录与组织管理流程改造成更标准的 SaaS 结构。

## 3. 系统关系

- `Rosterly / System B`
  - 多租户 SaaS 核心层。
  - 负责：店铺、员工、角色、排班、预约、订单、邮件模板、服务预设、管理后台。
- `System A`
  - `Rosterly` 的特别定制版前台。
  - 面向特定品牌客户，调用 `Rosterly Integration API` 完成预约链路。
- 结论：
  - `Rosterly` 是主产品。
  - `System A` 是基于 `Rosterly` 的定制业务壳层。

## 4. 登录与注册设计

### 4.1 登录方式

- 系统采用 `Discord OAuth` 作为注册与登录的统一入口。
- 不设计传统用户名密码注册作为正式主流程。
- 本地账号登录仅保留开发/测试用途，不作为正式产品方案。
- 最终口径（2026-03-24）：
  - C 端用户在 System A 触发登录，链路为 `A -> B 授权 -> A`。
  - 用户体验侧不暴露 B 的后台概念（无“去 B 登录”心智负担）。
  - 切换账号时，A 发起链路会重新触发认证，不依赖旧会话。

### 4.2 店长注册店铺

- 首次使用由 `店长` 发起注册。
- 店长通过 `Discord OAuth` 完成身份认证。
- 注册完成后，系统创建：
  - 一个新的 `Tenant / Shop`
  - 当前店长账号
  - 当前店长的初始 `ADMIN` 权限
- 店长是店铺的首个管理员，可进入 `Rosterly` 管理后台。

### 4.3 店员邀请注册

- 店员不能自由注册加入任意店铺。
- 由店长/管理员在后台生成邀请链接。
- 邀请链接至少包含：
  - 所属店铺标识
  - 目标角色（默认 `STAFF`，也可指定 `ADMIN`）
  - 过期时间
  - 一次性或可控次数使用限制
- 被邀请者点击链接后，通过 `Discord OAuth` 完成注册。
- 注册成功后：
  - 自动归属到对应店铺
  - 自动成为邀请指定角色
  - 初次进入对应功能页面

### 4.4 管理员授权其他管理员

- 店长/管理员可以将已有员工提升为管理员。
- 也可以直接生成“管理员邀请链接”，让被邀请者注册后即成为 `ADMIN`。
- 管理员授权属于租户内权限行为，不影响其他租户。

## 5. 角色定义与权限

### 5.0 角色补充（跨系统口径）

- `System B` 新增 `CONSUMER` 角色用于标识“由 A 端登录、且无租户绑定”的用户。
- `CONSUMER` 不属于员工体系，不应进入 B 员工后台能力。
- `STAFF/ADMIN` 仍只用于店铺员工/管理员运营体系。
- 当前角色映射（2026-03-25）：
  - A 管理员（`is_superuser` 或 `is_staff 且非 cast`）→ B `ADMIN`
  - A 员工（`is_cast=True`）→ B `STAFF`
  - A 普通用户 → B `CONSUMER`
- A 来源用户在 B 侧缺失租户时，默认绑定到 `Veludo` 公共租户（可配置）。

### 5.1 Staff（店员）

- 通过邀请链接注册加入店铺。
- 可管理自己的排班。
- 可查看自己的预约订单。
- 可完结自己的订单。
- 可编辑自己的基础个人资料。
- 不可取消订单。
- 不可查看全店数据。
- 不可编辑其他店员介绍资料。
- 不可编辑邮件模板、服务预设、记账信息。

### 5.2 Admin（店长 / 管理员）

- 店长是首个管理员。
- 管理员可被其他管理员授权。
- 可查看与管理租户内全部排班与订单。
- 可编辑店员介绍资料。
- 可管理员工账号与角色。
- 可授权其他管理员。
- 可使用记账功能。
- 可编辑邮件内容模板。
- 可管理服务预设。

## 6. 核心业务功能

### 6.1 与 System A 基本一致的核心能力

- 排班管理
- 预约管理
- 订单查看与状态管理
- 邮件通知
- 店员资料编辑

### 6.2 System B / Rosterly 作为主体新增或强化的能力

- 店铺创建与租户管理
- 邀请链接注册机制
- 店员/管理员组织关系管理
- 通用服务预设
- 记账功能
- 可复用的后台设计体系

## 7. 页面与模块设计

### 7.1 整体视觉风格

- `Rosterly` 整体设计风格采用当前管理员面板风格。
- 视觉基调参考现有 `tenant_dashboard`：
  - 简洁 SaaS 后台布局
  - 左侧导航 + 右侧内容区
  - 清晰卡片式信息分层
  - 浅色主界面、克制高亮色、偏专业的运营工具气质
- 新增页面应尽量沿用该设计体系，避免和 System A 的品牌前台视觉混淆。

### 7.2 Staff 可使用模块

- 排班管理
  - 管理自己的排班
  - 查看自己的预约占用情况
  - 支持周期排班与模板能力
- 预约订单
  - 查看自己的预约记录
  - 仅可完结自己的订单
- 个人资料
  - 编辑基础资料
  - 编辑与自身绑定 `Resource` 的基础展示信息（非全店统一管理项）
  - 个人前端页面统一要求（2026-03-25）：
    - `/home`、`/profile`、`/schedule`、`/bookings` 使用一致的顶部 Header 与导航结构
    - 顶部持续显示当前店铺名
    - 头像菜单使用点击展开（避免 hover 丢失）

### 7.3 Admin 可使用模块

- 店铺概览 Dashboard
- 全店排班查看与管理
- 全店订单查看与管理
- 店员资料与介绍资料编辑
- 角色管理
- 邀请链接管理
- 邮件模板编辑
- 服务预设管理
- 记账功能

## 8. 资料编辑边界

### 8.1 店员基础个人资料

- 由店员本人可编辑：
  - 用户名
  - 邮箱
  - 基础头像/基础资料
- 由系统维护、店员前端不可编辑：
  - Discord ID（来源于 OAuth 身份，作为稳定映射标识）

### 8.2 店员介绍资料

- 店员可编辑（仅自己的绑定 Resource）：
  - 自我介绍（`intro`）
  - 标签（`tags`）
  - 头像 URL / YouTube URL
  - 可接单时长模块（`allow_30/60/120_min`）
- 管理员可编辑（全店维度）：
  - 任意店员的展示资料与账号角色
  - 影响店铺统一展示质量的运营字段

## 9. 服务预设设计

- `Rosterly` 需要有店铺级服务预设能力。
- 服务预设由管理员维护。
- 每个服务预设至少包含：
  - 服务名称
  - 时长
  - 启用状态
  - 排序
- System B 的一般用户在预约时应明确选择某个服务。
- 预约订单与邮件通知应显示用户实际选择的服务名称。
- 若是 System A 定制版场景，可继续沿用“由预约开始/结束时间相减推导时长”的方式，不强依赖 System A 自身服务表结构。

## 10. 邮件功能设计

- 邮件能力由 `Rosterly` 统一提供。
- 邮件模板由管理员维护。
- 邮件内容支持动态变量。
- 一般化 `Rosterly` 场景下：
  - 邮件中的服务名优先使用用户实际选择的服务预设名称。
- System A 定制场景下：
  - 若未传特定服务预设，则可回退为根据预约时长动态生成服务文案。

## 11. 记账功能

- `Rosterly` 管理员侧需要显示记账功能。
- 该功能面向店长/管理员，不面向普通店员。
- 当前需求文档先定义为“管理员可见的店铺运营模块”，细节字段与报表结构可在后续专题文档中展开。

## 12. 页面访问原则

### 12.1 Staff

- 可访问：
  - 自己的排班页
  - 自己的订单页
  - 自己的个人资料页
- 不可访问：
  - 全店管理面板
  - 服务预设管理
  - 邮件模板管理
  - 邀请链接管理
  - 记账功能

### 12.2 Admin

- 可访问：
  - 全部 Staff 能访问的页面
  - 管理员 Dashboard
  - 全店排班/订单
  - 员工与管理员管理
  - 邮件模板编辑
  - 服务预设管理
  - 记账功能

## 13. 实施状态（2026-03-23）

- 已完成：System B 新增 ResourceProfile/ResourceMedia 作为 Cast 主数据落点。
- 已完成：System A 默认改为远端读取 Cast（`CAST_SOURCE=remote`），并保留本地回退开关。
- 已完成：System A 管理员编辑 CastProfile 后实时同步到 System B。
- 已完成：`sync_casts_to_system_b --only-active` 线上迁移成功（17/17）。
- 已完成（2026-03-24）：
  - System B 作为统一身份源（IdP）上线 `authorize/exchange`。
  - System A 改为 SSO 消费方（SP），完成 callback 映射与会话建立。
  - A/B 账号映射口径统一为 `saas_user_id -> discord_uid -> discord_id`。
  - B 中 A 端用户与员工用户完成角色区分（`CONSUMER` vs `STAFF/ADMIN`）。
- 已完成（2026-03-25）：
  - A 发起 SSO 时透传 `a_role=ADMIN|STAFF|CONSUMER`。
  - B 在 public SSO 流程按 `a_role` 同步 `role/is_staff`。
  - 增加 `backfill_a_sso_users` 一次性历史修正命令（支持 dry-run / apply / fallback 手工角色指定）。
  - 员工前端 `/home|/profile|/schedule|/bookings` 统一为同一 Header 设计语言。
  - 个人资料页中 `Discord ID` 调整为只读，后端不再接受该字段更新。
  - 员工端个人资料页已接入 `ResourceProfile` 扩展字段保存（intro/tags/avatar/youtube/allow_30/60/120）。

## 13. System A 作为定制版的保留原则

- System A 继续保留其品牌前台与特定交互。
- System A 预约场景可以不引入额外数据库迁移，只通过预约起止时间推导时长。
- 但 `Rosterly` 作为主产品，应具备完整的通用服务预设与用户选择服务的业务能力。
- 后续新增通用能力优先落在 `Rosterly`，再评估是否需要反向同步到 System A 定制版。

## 14. 验收方向

- 能由店长通过 Discord 完成店铺创建。
- 能由管理员生成邀请链接并邀请店员/管理员注册。
- 店员注册后能进入自己的工作页面。
- 店员只能操作自己的排班、订单与基础资料。
- 店员端 `Discord ID` 不可手动编辑。
- 店员只能完结自己的订单，不可取消订单。
- 管理员可管理全店排班、订单、员工资料、服务预设、邮件模板与记账模块。
- 整体界面风格统一采用当前管理员面板风格。
- System B 在一般化预约链路中可让用户选择具体服务，并在邮件中显示该服务名称。
